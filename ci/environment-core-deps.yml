# Conda environment for testing xmovie in CI
name: xmovie-test-core
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python
  #
  # xmovie core
  - dask-core
  - matplotlib
  - numpy
  - xarray
  #
  - ffmpeg
  #
  # Tests
  - opencv
  - pillow
  - pytest
  - pytest-cov
